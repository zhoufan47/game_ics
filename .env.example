# 复制本文件为 .env，并在本地填入真实的 RAWG API key。
# .env 已被 .gitignore 忽略，不会被提交到仓库。
# 从 https://rawg.io/apidocs 申请 key。

RAWG_API_KEY=your_rawg_api_key_here

# --- 可选：代理配置 ---
# 全部留空表示直连。任一项非空即生效：
#   - RAWG_PROXY：同时作为 http/https 代理，最常用；
#   - RAWG_HTTP_PROXY / RAWG_HTTPS_PROXY：按 scheme 单独覆盖，优先级高于 RAWG_PROXY。
# 协议支持 http / https / socks5。URL 内可直接携带认证信息，例如
#   http://user:password@127.0.0.1:7890
# RAWG_PROXY=
# RAWG_HTTP_PROXY=
# RAWG_HTTPS_PROXY=